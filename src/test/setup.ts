import '@testing-library/jest-dom';

// This file contains setup code that will run before each test
// It's referenced in the vite.config.ts file

// You can add global mocks here
// For example, if you need to mock window.matchMedia:
/*
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: vi.fn().mockImplementation(query => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: vi.fn(),
    removeListener: vi.fn(),
    addEventListener: vi.fn(),
    removeEventListener: vi.fn(),
    dispatchEvent: vi.fn(),
  })),
});
*/

// You can also add global test utilities here
