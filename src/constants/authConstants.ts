// --- Constants ---
export const USER_TOKEN_KEY = "user_token";
export const USER_DATA_KEY = "user_data";

// --- Types ---
import type {UserInformation, AuthState, AuthContextType} from '../types/userTypes';
